mutation CreateEvenement (
  $title: String!,
  $description: String,
  $isValid: Boolean
) {
  createEvenement(data :{
    title: $title,
    description: $description,
    isValid: $isValid
  }) {
    id
  }
}